<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView android:id="@+id/txtName" android:text="Tên khách hàng:" />
        <TextView android:id="@+id/txtPhone" android:text="SĐT:" />
        <TextView android:id="@+id/txtAddress" android:text="Địa chỉ:" />
        <TextView android:id="@+id/txtOrderTime" android:text="Thời gian đặt:" />
        <TextView android:id="@+id/txtTotal" android:text="Tổng tiền:" />
        <TextView android:id="@+id/txtStatus" android:text="Trạng thái đơn hàng:" />

        <TextView android:text="Sản phẩm đã mua:" android:layout_marginTop="16dp" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerItems"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:nestedScrollingEnabled="false" />
    </LinearLayout>
</ScrollView>
